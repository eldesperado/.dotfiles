- defaults:
    link:
        relink: true
        create: true
    shell:
        stdin: true
        stdout: true
        stderr: true

- clean: ['~', '~/.setup']

- shell:
    - 
      - "sleep 0"
      - "Installing TrungPNN's dotfiles"
    - 
      command: 
        - "git pull"
        - "Updating dotfiles"
      stderr: true
      stdout: true
    - 
      command: 
        - "git submodule update --init --recursive --quiet"
        - "Installing submodules"
      stderr: true
      stdout: true

- link:
    # Setup
    ~/.setup:
        force: true
        path: setup

    # Bash
    ~/.profile: profile
    ~/.bash_profile: bash_profile
    ~/.bashrc: bashrc

    # Git:
    ~/.gitignore_global: git/gitignore_global
    ~/.gitconfig.local: git/gitconfig.local
    ~/.stCommitMsg: git/stCommitMsg

    # Vim:
    ~/.vimrc:
        force: true
        path: vim/vimrc

    # zsh/prezto:
    ~/.zprezto:
        force: true
        path: zsh-prezto/prezto/
    ~/.zlogin: 
        force: true
        path: zsh-prezto/zlogin
    ~/.zlogout: 
        force: true
        path: zsh-prezto/zlogout
    ~/.zpreztorc: 
        force: true
        path: zsh-prezto/zpreztorc
    ~/.zprofile: 
        force: true
        path: zsh-prezto/zprofile
    ~/.zshenv: 
        force: true
        path: zsh-prezto/zshenv
    ~/.zshrc: 
        force: true
        path: zsh-prezto/zshrc

    # mackup:
    ~/.mackup.cfg: mackup.cfg

    # Hammerspoon:
    ~/.hammerspoon: 
        force: true
        path: hammerspoon
    
    # vscode
    "~/Library/Application Support/Code/User/settings.json":
        path: setup/vscode/settings.json
        create: true
        force: true
    "~/Library/Application Support/Code/User/snippets":
        path: setup/vscode/snippets
        create: true
        force: true

- shell:
    # font
    - 
      command: 
        - sh ~/.setup/fonts/install.sh
        - "Installing fonts(Powerline"
        - "Nerd and more)"
    
    # Git
    - 
      command: 
        - sh ~/.setup/git/install.sh

    # Homebrew
    - 
      command: 
        - sh ~/.setup/homebrew/install.sh

    # vscode
    - 
      command: 
        - sh ~/.setup/vscode/install.sh
    